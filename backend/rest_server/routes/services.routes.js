const express = require("express");
// const { queryLocationController } = require("../controller/services/queryLocation");

const router = express.Router();
// router.get("/query-location", queryLocationController);
module.exports = router;