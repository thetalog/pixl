package com.pixl.livestream.security;

public enum LivePermission {
    CREATE_STREAM,
    START_STREAM,
    END_STREAM,
    PUBLISH,
    JOIN_STREAM,
    LEAVE_STREAM,
    COMMENT,
    LIKE,
    MUTE_VIEWER,
    REMOVE_VIEWER,
    DELETE_COMMENT
}
